# Derex will take a file with this name into consideration
# when the runmode is production
version: "3.5"
services:
  httpserver:
    image: abiosoft/caddy:1.0.3-no-stats
    ports:
      - 127.0.0.1:80:2015
    volumes:
      - ../Caddyfile:/etc/Caddyfile
    networks:
      - derex
