stages:

  - stage: Push
    variables:
    - group: Docker credentials
    jobs:

    - job: hawthorn
      pool:
        vmImage: 'ubuntu-18.04'
      variables:
        - name: DOCKER_IMAGES_SLUG
          value: hawthorn
      steps:
        - template: push_single_version.yml

    - job: ironwood
      pool:
        vmImage: 'ubuntu-18.04'
      variables:
        - name: DOCKER_IMAGES_SLUG
          value: ironwood
      steps:
        - template: push_single_version.yml

    - job: juniper
      pool:
        vmImage: 'ubuntu-18.04'
      variables:
        - name: DOCKER_IMAGES_SLUG
          value: hawthorn
      steps:
        - template: push_single_version.yml
